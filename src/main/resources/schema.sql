CREATE TABLE reservation(
                id IDENTITY NOT NULL,
                date DATE,
                time TIME,
                name VARCHAR(20),
                PRIMARY KEY (id)
                );
